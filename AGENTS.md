# Project agent instructions

- The project-local `typesafe-ai` skill is opt-in. Use it only when the user explicitly asks to use TypeSafe in the current request. Do not invoke it merely because a task matches the skill's general description.
- Keep TypeSafe usage and any TypeSafe-related changes scoped to this repository.
